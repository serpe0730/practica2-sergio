name: Test
on: push
jobs:
  tarea1:
    name: Tarea 1
    runs-on: ubuntu-latest
    steps:
      - name: create GitHub Issue
        uses: DanielBarnes/create-issue-action@v1
        with:
          title: "Push asociado"
